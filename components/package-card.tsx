import { Check, Zap } from "lucide-react";
import type { PackagePlan } from "@/types";
export function PackageCard({ plan }: { plan: PackagePlan }) { return <article className={`relative flex h-full flex-col rounded-2xl border bg-white p-6 shadow-card transition duration-300 hover:-translate-y-2 hover:shadow-xl ${plan.featured ? "border-sky ring-1 ring-sky" : "border-slate-100"}`}>{plan.featured && <span className="absolute -top-3 left-1/2 -translate-x-1/2 rounded-full bg-sky px-4 py-1 text-xs font-bold text-white">MOST POPULAR</span>}<div className="mb-6 flex items-start justify-between"><div><p className="text-sm font-bold uppercase tracking-[.16em] text-sky">{plan.name}</p><h3 className="mt-3 inline-flex rounded-lg bg-gradient-to-r from-blue-50 to-sky-100 px-3 py-1.5 text-2xl font-extrabold tracking-tight text-ink ring-1 ring-sky/15">{plan.speed}</h3></div><span className="grid size-10 place-items-center rounded-xl bg-blue-50 text-sky"><Zap size={20}/></span></div><p className="min-h-12 text-sm leading-6 text-slate-500">{plan.description}</p><div className="my-6 border-y border-slate-100 py-5"><span className="text-3xl font-extrabold text-ink">{plan.price.amount}</span><span className="ml-1 text-sm text-slate-500">/ {plan.price.billing_cycle}</span><p className="mt-1 text-xs text-slate-400">{plan.price.note}</p></div><ul className="mb-7 space-y-3">{plan.whats_included.map(item => <li key={item} className="flex items-center gap-2.5 text-sm text-slate-600"><Check size={16} className="shrink-0 text-sky"/> {item}</li>)}</ul><a href={plan.action.url} className={`mt-auto block rounded-lg py-3 text-center text-sm font-bold transition ${plan.featured ? "bg-sky text-white hover:bg-[#1266d6]" : "bg-blue-50 text-sky hover:bg-ink hover:text-white"}`}>{plan.action.text}</a></article>; }
